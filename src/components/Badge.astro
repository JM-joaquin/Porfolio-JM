<!-- components/Badge.astro -->
<style>
    /* Estilos para Badge y animación changeColor */
    @keyframes changeColor {
      0% { background-color: bg-blue-100; }
      50% { background-color: rgb(182, 182, 182); }
      100% { background-color: bg-blue-100; }
    }
  
    .Badge {
      display: inline-block;
      animation: changeColor 3s infinite;
    }
  </style>
  
  <span class="Badge bg-blue-100 text-blue-800 text-sm font-medium me-2 px-2 py-0.5 rounded dark:bg-blue-900 dark:text-blue-300">
    <slot />
</span>